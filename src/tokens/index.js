import breakpoints from './breakpoints';
import colors from './colors';
import media from './media';

const tokens = {
  breakpoints,
  colors,
  media,
};

export { tokens as default, breakpoints, colors, media };
