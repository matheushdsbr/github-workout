import React from 'react';
import { SearchContainer } from './styles';

const Search = () => (
  <div>
    <SearchContainer>
      <h1>Search</h1>
    </SearchContainer>
  </div>
);

export default Search;
